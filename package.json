{
    "name": "@mathiasgrimm/laravel-puff",
    "description": "Framework-agnostic keep-alive stubs for laravel-puff (dev tooling only; the stubs are published via composer).",
    "private": true,
    "type": "module",
    "scripts": {
        "types:check": "tsc --noEmit",
        "format": "prettier --write resources/js",
        "format:check": "prettier --check resources/js"
    },
    "devDependencies": {
        "@types/react": "^19.0.0",
        "prettier": "^3.4.2",
        "react": "^19.0.0",
        "typescript": "^5.7.0",
        "vue": "^3.5.13"
    }
}
